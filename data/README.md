# Data

This folder contains publicly-available data used by the source code in the repository (including the dashboard). Note that some modelling inputs cannot be made publicly-available, so they are not included here. Those data would be stored locally in `./wd/in/` which is ignored by GitHub (i.e. so do not become public).  
